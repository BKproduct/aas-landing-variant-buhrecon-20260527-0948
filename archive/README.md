# Legacy AAS landing snapshot

The pre-localization landing is preserved in Git as the annotated tag
`legacy-production-before-i18n-2026-07-21`.

It is retained as a rollback/reference point. The current public website is
https://ai-automation.studio; the GitHub Pages preview is legacy and is not the
production site.

To inspect the preserved version locally:

```bash
git show legacy-production-before-i18n-2026-07-21:index.html
```
